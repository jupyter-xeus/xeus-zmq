name: xeus-zmq
channels:
  - conda-forge
dependencies:
  # Build dependencies
  - cmake
  - cxx-compiler
  - pkg-config
  - ninja
  # Host dependencies
  - zeromq
  - zeromq-static
  - cppzmq
  - xeus>=6,<7
  - OpenSSL 
  - libopenssl-static
  - nlohmann_json=3.12.0
  # Test dependencies
  - doctest >= 2.4.6
  - pytest
  - jupyter_kernel_test>=0.6,<0.7
